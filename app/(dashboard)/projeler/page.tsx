import { redirect } from 'next/navigation'

export default async function ProjelerPage() {
  // Projeler sayfası artık ana sayfa, buraya gelenleri ana sayfaya yönlendir
  redirect('/')
}















